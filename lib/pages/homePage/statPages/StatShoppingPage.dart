import 'package:flutter/material.dart';

class StatShoppingPage extends StatelessWidget {
  const StatShoppingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
